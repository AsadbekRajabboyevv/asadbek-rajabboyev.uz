<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard | Portfolio</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .tab-content { display: none; }
        .tab-content.active { display: block; }
    </style>
</head>
<body class="bg-slate-900 text-slate-100 font-['Poppins'] min-h-screen flex flex-col md:flex-row">
    
    <!-- Mobile Top Navigation Header -->
    <header class="md:hidden bg-[#00072D] border-b border-slate-800 p-4 flex items-center justify-between sticky top-0 z-50">
        <div class="flex items-center gap-3">
            <div class="w-8 h-8 rounded-lg bg-blue-600 flex items-center justify-center font-bold text-white text-sm">AR</div>
            <h1 class="font-bold text-sm text-white">Admin Panel</h1>
        </div>
        <button id="mobile-toggle" onclick="toggleSidebar()" class="text-slate-300 hover:text-white p-2 text-lg focus:outline-none">
            <i class="fas fa-bars"></i>
        </button>
    </header>

    <!-- Sidebar (Collapsible on Mobile, Fixed Sidebar on Desktop) -->
    <aside id="sidebar" class="hidden md:flex fixed md:relative inset-y-0 left-0 z-40 w-full md:w-64 bg-[#00072D] border-r border-slate-800 p-6 flex-col justify-between flex-shrink-0 transition-all duration-300">
        <div>
            <div class="hidden md:flex items-center gap-3 mb-8">
                <div class="w-10 h-10 rounded-xl bg-blue-600 flex items-center justify-center font-bold text-white">AR</div>
                <div>
                    <h2 class="font-bold text-sm text-white">Admin Panel</h2>
                    <p class="text-[10px] text-blue-400">Dynamic Management</p>
                </div>
            </div>

            <nav class="space-y-1.5 text-sm">
                <button onclick="showTab('settings')" id="nav-settings" class="nav-btn w-full flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-800 transition text-left text-slate-300">
                    <i class="fas fa-cog text-blue-400"></i> Settings
                </button>
                <button onclick="showTab('about')" id="nav-about" class="nav-btn w-full flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-800 transition text-left text-slate-300">
                    <i class="fas fa-user text-blue-400"></i> About Paragraphs
                </button>
                <button onclick="showTab('skills')" id="nav-skills" class="nav-btn w-full flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-800 transition text-left text-slate-300">
                    <i class="fas fa-code text-blue-400"></i> Skills
                </button>
                <button onclick="showTab('projects')" id="nav-projects" class="nav-btn w-full flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-800 transition text-left text-slate-300">
                    <i class="fas fa-project-diagram text-blue-400"></i> Projects
                </button>
                <button onclick="showTab('experience')" id="nav-experience" class="nav-btn w-full flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-800 transition text-left text-slate-300">
                    <i class="fas fa-briefcase text-blue-400"></i> Experience
                </button>
                <button onclick="showTab('education')" id="nav-education" class="nav-btn w-full flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-800 transition text-left text-slate-300">
                    <i class="fas fa-graduation-cap text-blue-400"></i> Education
                </button>
            </nav>
        </div>

        <div class="pt-6 border-t border-slate-800 space-y-2 mt-6 md:mt-0">
            <a href="/" target="_blank" class="block w-full py-2.5 px-4 bg-blue-600/20 text-blue-300 text-xs font-semibold rounded-xl text-center hover:bg-blue-600 hover:text-white transition">
                <i class="fas fa-external-link-alt mr-2"></i> View Site
            </a>
            <a href="/admin/logout" class="block w-full py-2.5 px-4 bg-rose-600/20 text-rose-300 text-xs font-semibold rounded-xl text-center hover:bg-rose-600 hover:text-white transition">
                <i class="fas fa-sign-out-alt mr-2"></i> Log Out
            </a>
        </div>
    </aside>

    <!-- Main Content Body -->
    <main class="flex-1 p-4 md:p-10 overflow-y-auto max-h-screen">
        <!-- Settings Tab -->
        <div id="tab-settings" class="tab-content space-y-6">
            <h2 class="text-xl md:text-2xl font-bold border-b border-slate-800 pb-3">General Settings</h2>
            <form action="/admin/settings" method="POST" enctype="multipart/form-data" class="space-y-6 max-w-4xl">
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 md:gap-6 bg-slate-800/40 p-4 md:p-6 rounded-2xl border border-slate-700">
                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Full Name</label>
                        <input type="text" name="full_name" value="<%= settings.full_name %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Birth Date</label>
                        <input type="text" name="birth_date" value="<%= settings.birth_date %>" placeholder="29 March 2006" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>

                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Job Title (EN)</label>
                        <input type="text" name="job_title_en" value="<%= settings.job_title_en %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Job Title (RU)</label>
                        <input type="text" name="job_title_ru" value="<%= settings.job_title_ru %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Job Title (UZ)</label>
                        <input type="text" name="job_title_uz" value="<%= settings.job_title_uz %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Position Label (EN)</label>
                        <input type="text" name="position_en" value="<%= settings.position_en %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>

                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Location (EN)</label>
                        <input type="text" name="location_en" value="<%= settings.location_en %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Location Google Map URL</label>
                        <input type="text" name="location_url" value="<%= settings.location_url %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>

                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Current Company Name</label>
                        <input type="text" name="current_company" value="<%= settings.current_company %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Current Company URL</label>
                        <input type="text" name="company_url" value="<%= settings.company_url %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>

                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Primary Email</label>
                        <input type="email" name="email_1" value="<%= settings.email_1 %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Secondary Email</label>
                        <input type="email" name="email_2" value="<%= settings.email_2 %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-300 uppercase mb-1.5">Phone Number</label>
                        <input type="text" name="phone" value="<%= settings.phone %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white">
                    </div>
                    <div class="sm:col-span-2">
                        <div class="flex items-center justify-between mb-2">
                            <label class="block text-xs font-semibold text-slate-300 uppercase">Active Website Theme</label>
                            <button type="button" onclick="document.getElementById('custom-theme-modal').classList.remove('hidden')" class="text-xs font-bold text-blue-400 hover:text-blue-300 flex items-center gap-1.5 bg-blue-900/30 px-3 py-1.5 rounded-lg border border-blue-500/20">
                                <i class="fas fa-plus-circle"></i> Create New Custom Theme
                            </button>
                        </div>
                        <select name="theme" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-white outline-none">
                            <% (themes || []).forEach(t => { %>
                                <option value="<%= t.code %>" <%= (settings.theme === t.code || (!settings.theme && t.code === 'cyber_dark')) ? 'selected' : '' %>>
                                    <%= t.name %> <%= t.is_custom ? '🎨 (Custom Created)' : '' %>
                                </option>
                            <% }) %>
                        </select>

                        <!-- Theme Preview Cards Grid -->
                        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 mt-4">
                            <% (themes || []).forEach(t => { %>
                                <div class="p-3.5 rounded-xl border <%= settings.theme === t.code ? 'border-blue-500 bg-blue-950/30' : 'border-slate-800 bg-slate-900/40' %> flex flex-col justify-between space-y-3">
                                    <div class="flex items-center justify-between">
                                        <span class="text-xs font-bold text-white flex items-center gap-2">
                                            <span class="w-3 h-3 rounded-full inline-block" style="background-color: <%= t.accent_color %>;"></span>
                                            <%= t.name %>
                                        </span>
                                        <% if (settings.theme === t.code) { %>
                                            <span class="text-[10px] uppercase font-extrabold bg-blue-600 text-white px-2 py-0.5 rounded-full">Active</span>
                                        <% } %>
                                    </div>
                                    <div class="h-6 rounded-lg flex items-center px-3 text-xs justify-between" style="background-color: <%= t.bg_color %>; color: <%= t.fg_color %>; border: 1px solid <%= t.glass_border %>;">
                                        <span>Preview</span>
                                        <span class="font-bold" style="color: <%= t.accent_color %>;">Accent</span>
                                    </div>
                                    <% if (t.is_custom) { %>
                                        <div class="flex justify-end pt-1">
                                            <button type="submit" formaction="/admin/themes/delete/<%= t.id %>" onclick="return confirm('Delete this custom theme?')" class="text-[11px] font-semibold text-rose-400 hover:text-rose-300">
                                                <i class="fas fa-trash"></i> Delete
                                            </button>
                                        </div>
                                    <% } %>
                                </div>
                            <% }) %>
                        </div>
                    </div>

                    <div class="sm:col-span-2 p-4 bg-slate-900/60 rounded-xl border border-slate-700/60 space-y-3">
                        <label class="block text-xs font-bold text-blue-400 uppercase">Background Music File (MP3 / WAV)</label>
                        <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4">
                            <div class="flex items-center gap-3">
                                <i class="fas fa-music text-blue-400 text-xl"></i>
                                <div>
                                    <p class="text-xs text-slate-300 font-medium">Current Track: <span class="text-blue-300 font-bold"><%= settings.music_file || 'No music uploaded' %></span></p>
                                </div>
                            </div>
                            <input type="file" name="music_file" accept="audio/*" class="block text-xs text-slate-400 file:mr-3 file:py-1.5 file:px-3 file:rounded-lg file:border-0 file:text-xs file:font-semibold file:bg-blue-600 file:text-white hover:file:bg-blue-500">
                        </div>
                        <% if (settings.music_file) { %>
                            <div class="flex items-center gap-2 pt-2 border-t border-slate-800">
                                <input type="checkbox" name="delete_music" value="1" id="delete_music" class="rounded border-slate-700 text-blue-600">
                                <label for="delete_music" class="text-xs text-rose-400 font-semibold cursor-pointer">Delete current music file</label>
                            </div>
                        <% } %>
                    </div>
                </div>

                <button type="submit" class="w-full sm:w-auto px-8 py-3 bg-blue-600 hover:bg-blue-500 rounded-xl font-bold text-sm text-white shadow-lg transition">Save Settings</button>
            </form>

            <!-- Custom Theme Creator Modal -->
            <div id="custom-theme-modal" class="fixed inset-0 z-50 hidden bg-black/70 backdrop-blur-md flex items-center justify-center p-4">
                <div class="bg-slate-900 border border-slate-700 rounded-2xl max-w-lg w-full p-6 space-y-6 shadow-2xl">
                    <div class="flex items-center justify-between border-b border-slate-800 pb-4">
                        <h3 class="text-lg font-bold text-white flex items-center gap-2">
                            <i class="fas fa-palette text-blue-400"></i> Create Custom Theme
                        </h3>
                        <button type="button" onclick="document.getElementById('custom-theme-modal').classList.add('hidden')" class="text-slate-400 hover:text-white text-lg">&times;</button>
                    </div>

                    <form action="/admin/themes/create" method="POST" class="space-y-4">
                        <div>
                            <label class="block text-xs font-bold text-slate-300 uppercase mb-1">Theme Name</label>
                            <input type="text" name="name" required placeholder="e.g. Cyberpunk Magenta" class="w-full bg-slate-950 border border-slate-700 rounded-xl px-3.5 py-2 text-sm text-white">
                        </div>

                        <div class="grid grid-cols-3 gap-3">
                            <div>
                                <label class="block text-xs font-bold text-slate-300 uppercase mb-1">Background</label>
                                <input type="color" name="bg_color" value="#0d1117" class="w-full h-10 bg-slate-950 border border-slate-700 rounded-xl p-1 cursor-pointer">
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-slate-300 uppercase mb-1">Text Color</label>
                                <input type="color" name="fg_color" value="#f0f6fc" class="w-full h-10 bg-slate-950 border border-slate-700 rounded-xl p-1 cursor-pointer">
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-slate-300 uppercase mb-1">Accent Color</label>
                                <input type="color" name="accent_color" value="#58a6ff" class="w-full h-10 bg-slate-950 border border-slate-700 rounded-xl p-1 cursor-pointer">
                            </div>
                        </div>

                        <div>
                            <label class="block text-xs font-bold text-slate-300 uppercase mb-1">Glass Fill (RGBA CSS)</label>
                            <input type="text" name="glass_bg" value="rgba(22, 27, 34, 0.6)" class="w-full bg-slate-950 border border-slate-700 rounded-xl px-3.5 py-2 text-sm text-white">
                        </div>

                        <div>
                            <label class="block text-xs font-bold text-slate-300 uppercase mb-1">Glass Border (RGBA CSS)</label>
                            <input type="text" name="glass_border" value="rgba(56, 139, 253, 0.25)" class="w-full bg-slate-950 border border-slate-700 rounded-xl px-3.5 py-2 text-sm text-white">
                        </div>

                        <div class="flex justify-end gap-3 pt-2">
                            <button type="button" onclick="document.getElementById('custom-theme-modal').classList.add('hidden')" class="px-5 py-2 bg-slate-800 hover:bg-slate-700 rounded-xl text-xs font-bold text-slate-300">Cancel</button>
                            <button type="submit" class="px-6 py-2 bg-blue-600 hover:bg-blue-500 rounded-xl text-xs font-bold text-white shadow-lg">Save & Install Theme</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <!-- About Tab -->
        <div id="tab-about" class="tab-content space-y-6">
            <h2 class="text-xl md:text-2xl font-bold border-b border-slate-800 pb-3">About Paragraphs</h2>
            
            <!-- Add New About Paragraph Form -->
            <div class="bg-blue-950/40 p-4 md:p-6 rounded-2xl border border-blue-500/30 mb-6 max-w-4xl">
                <h3 class="text-base md:text-lg font-bold text-blue-300 mb-3"><i class="fas fa-plus-circle mr-2"></i>Add New Paragraph</h3>
                <form action="/admin/about/create" method="POST" class="space-y-3">
                    <div class="grid grid-cols-1 gap-3">
                        <div><label class="text-xs font-bold text-blue-400">English Text</label><textarea name="text_en" required rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"></textarea></div>
                        <div><label class="text-xs font-bold text-blue-400">Russian Text</label><textarea name="text_ru" required rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"></textarea></div>
                        <div><label class="text-xs font-bold text-blue-400">Uzbek Text</label><textarea name="text_uz" required rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"></textarea></div>
                    </div>
                    <div class="flex items-center justify-between pt-2">
                        <div><label class="text-xs font-bold text-blue-400 mr-2">Sort Order</label><input type="number" name="sort_order" value="0" class="w-20 bg-slate-900 border border-slate-700 rounded-xl px-3 py-1.5 text-sm"></div>
                        <button type="submit" class="px-6 py-2 bg-blue-600 hover:bg-blue-500 rounded-xl font-bold text-xs text-white shadow-lg">Add Paragraph</button>
                    </div>
                </form>
            </div>

            <!-- Existing Paragraphs List -->
            <div class="space-y-4 max-w-4xl">
                <% about.forEach((p, idx) => { %>
                    <form action="/admin/about/update/<%= p.id %>" method="POST" class="bg-slate-800/40 p-4 md:p-6 rounded-2xl border border-slate-700 space-y-3">
                        <div class="grid grid-cols-1 gap-3">
                            <div><label class="text-xs font-bold text-blue-400">English Text</label><textarea name="text_en" rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"><%= p.text_en %></textarea></div>
                            <div><label class="text-xs font-bold text-blue-400">Russian Text</label><textarea name="text_ru" rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"><%= p.text_ru %></textarea></div>
                            <div><label class="text-xs font-bold text-blue-400">Uzbek Text</label><textarea name="text_uz" rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"><%= p.text_uz %></textarea></div>
                        </div>
                        <div class="flex flex-wrap items-center justify-between gap-3">
                            <input type="number" name="sort_order" value="<%= p.sort_order %>" class="w-20 bg-slate-900 border border-slate-700 rounded-xl px-3 py-1.5 text-sm">
                            <div class="flex gap-2">
                                <button type="submit" class="px-4 py-2 bg-blue-600 rounded-xl text-xs font-bold text-white">Update</button>
                                <button type="submit" formaction="/admin/about/delete/<%= p.id %>" class="px-4 py-2 bg-rose-600 rounded-xl text-xs font-bold text-white">Delete</button>
                            </div>
                        </div>
                    </form>
                <% }) %>
            </div>
        </div>

        <!-- Skills Tab -->
        <div id="tab-skills" class="tab-content space-y-6">
            <h2 class="text-xl md:text-2xl font-bold border-b border-slate-800 pb-3">Skills Management</h2>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <% skills.forEach((skill) => { %>
                    <form action="/admin/skills/update/<%= skill.id %>" method="POST" class="bg-slate-800/40 p-4 rounded-2xl border border-slate-700 space-y-3">
                        <input type="text" name="name" value="<%= skill.name %>" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-1.5 text-sm text-white">
                        <div class="flex items-center gap-2">
                            <i class="<%= skill.icon_class %> text-xl text-blue-400"></i>
                            <input type="text" name="icon_class" value="<%= skill.icon_class %>" class="flex-1 bg-slate-900 border border-slate-700 rounded-lg px-3 py-1.5 text-xs text-white">
                        </div>
                        <div class="flex items-center gap-2">
                            <input type="number" name="percentage" value="<%= skill.percentage %>" min="0" max="100" class="w-20 bg-slate-900 border border-slate-700 rounded-lg px-3 py-1.5 text-xs text-white">
                            <span class="text-xs text-slate-400">%</span>
                        </div>
                        <div class="flex justify-between pt-2">
                            <button type="submit" class="px-3.5 py-1.5 bg-blue-600 rounded-lg text-xs font-bold text-white">Save</button>
                            <button type="submit" formaction="/admin/skills/delete/<%= skill.id %>" class="px-3.5 py-1.5 bg-rose-600 rounded-lg text-xs font-bold text-white">Delete</button>
                        </div>
                    </form>
                <% }) %>
            </div>
        </div>

        <!-- Projects Tab -->
        <div id="tab-projects" class="tab-content space-y-6">
            <h2 class="text-xl md:text-2xl font-bold border-b border-slate-800 pb-3">Projects Management</h2>

            <!-- Add New Project Form -->
            <div class="bg-blue-950/40 p-4 md:p-6 rounded-2xl border border-blue-500/30 mb-8 max-w-4xl">
                <h3 class="text-base md:text-lg font-bold text-blue-300 mb-4"><i class="fas fa-plus-circle mr-2"></i>Add New Project</h3>
                <form action="/admin/projects/create" method="POST" enctype="multipart/form-data" class="space-y-4">
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 md:gap-4">
                        <div><label class="text-xs font-bold text-blue-400">Title (EN)</label><input type="text" name="title_en" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Title (RU)</label><input type="text" name="title_ru" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Title (UZ)</label><input type="text" name="title_uz" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 md:gap-4">
                        <div><label class="text-xs font-bold text-blue-400">Description (EN)</label><textarea name="description_en" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></textarea></div>
                        <div><label class="text-xs font-bold text-blue-400">Description (RU)</label><textarea name="description_ru" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></textarea></div>
                        <div><label class="text-xs font-bold text-blue-400">Description (UZ)</label><textarea name="description_uz" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></textarea></div>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 md:gap-4">
                        <div><label class="text-xs font-bold text-blue-400">GitHub URL</label><input type="text" name="github_url" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Technologies (JSON or Comma separated)</label><input type="text" name="technologies" placeholder="Java, Spring Boot, PostgreSQL" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                    </div>
                    <div>
                        <label class="block text-xs font-bold text-blue-400 mb-2">Project Image File</label>
                        <input type="file" name="image" accept="image/*" class="block w-full text-xs text-slate-400 file:mr-4 file:py-2 file:px-4 file:rounded-xl file:border-0 file:text-xs file:font-semibold file:bg-blue-600 file:text-white hover:file:bg-blue-500">
                    </div>
                    <button type="submit" class="w-full sm:w-auto px-6 py-2.5 bg-blue-600 hover:bg-blue-500 rounded-xl font-bold text-xs text-white shadow-lg">Upload & Add Project</button>
                </form>
            </div>

            <!-- Existing Projects List -->
            <div class="space-y-6 max-w-4xl">
                <% projects.forEach((proj) => { %>
                    <form action="/admin/projects/update/<%= proj.id %>" method="POST" enctype="multipart/form-data" class="bg-slate-800/40 p-4 md:p-6 rounded-2xl border border-slate-700 space-y-4">
                        <input type="hidden" name="image" value="<%= proj.image %>">
                        <div class="flex items-center gap-4 border-b border-slate-700/60 pb-4">
                            <% if (proj.image) { %>
                                <img src="/uploads/<%= proj.image %>" alt="<%= proj.title_en %>" class="w-16 h-12 md:w-20 md:h-14 object-cover rounded-lg border border-slate-700">
                            <% } %>
                            <div>
                                <h4 class="font-bold text-sm text-white"><%= proj.title_en %></h4>
                                <p class="text-xs text-slate-400">Current file: <span class="text-blue-300"><%= proj.image || 'None' %></span></p>
                            </div>
                        </div>

                        <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 md:gap-4">
                            <div><label class="text-xs font-bold text-blue-400">Title (EN)</label><input type="text" name="title_en" value="<%= proj.title_en %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                            <div><label class="text-xs font-bold text-blue-400">Title (RU)</label><input type="text" name="title_ru" value="<%= proj.title_ru %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                            <div><label class="text-xs font-bold text-blue-400">Title (UZ)</label><input type="text" name="title_uz" value="<%= proj.title_uz %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        </div>

                        <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 md:gap-4">
                            <div><label class="text-xs font-bold text-blue-400">Description (EN)</label><textarea name="description_en" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"><%= proj.description_en %></textarea></div>
                            <div><label class="text-xs font-bold text-blue-400">Description (RU)</label><textarea name="description_ru" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"><%= proj.description_ru %></textarea></div>
                            <div><label class="text-xs font-bold text-blue-400">Description (UZ)</label><textarea name="description_uz" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"><%= proj.description_uz %></textarea></div>
                        </div>

                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 md:gap-4">
                            <div><label class="text-xs font-bold text-blue-400">GitHub URL</label><input type="text" name="github_url" value="<%= proj.github_url %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                            <div><label class="text-xs font-bold text-blue-400">Technologies (JSON or Comma separated)</label><input type="text" name="technologies" value='<%= typeof proj.technologies === "object" ? JSON.stringify(proj.technologies) : proj.technologies %>' class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        </div>

                        <div>
                            <label class="block text-xs font-bold text-blue-400 mb-2">Replace Image File</label>
                            <input type="file" name="image" accept="image/*" class="block w-full text-xs text-slate-400 file:mr-4 file:py-2 file:px-4 file:rounded-xl file:border-0 file:text-xs file:font-semibold file:bg-blue-600 file:text-white hover:file:bg-blue-500">
                        </div>

                        <div class="flex justify-between pt-2">
                            <button type="submit" class="px-5 py-2 bg-blue-600 rounded-xl text-xs font-bold text-white">Update Project</button>
                            <button type="submit" formaction="/admin/projects/delete/<%= proj.id %>" class="px-5 py-2 bg-rose-600 rounded-xl text-xs font-bold text-white">Delete</button>
                        </div>
                    </form>
                <% }) %>
            </div>
        </div>

        <!-- Experience Tab -->
        <div id="tab-experience" class="tab-content space-y-6">
            <h2 class="text-xl md:text-2xl font-bold border-b border-slate-800 pb-3">Experience Timeline</h2>
            
            <!-- Add New Experience Form -->
            <div class="bg-blue-950/40 p-4 md:p-6 rounded-2xl border border-blue-500/30 mb-6 max-w-4xl">
                <h3 class="text-base md:text-lg font-bold text-blue-300 mb-3"><i class="fas fa-plus-circle mr-2"></i>Add New Experience</h3>
                <form action="/admin/experience/create" method="POST" class="space-y-4">
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                        <div><label class="text-xs font-bold text-blue-400">Company (EN)</label><input type="text" name="company_en" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Company (RU)</label><input type="text" name="company_ru" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Company (UZ)</label><input type="text" name="company_uz" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                        <div><label class="text-xs font-bold text-blue-400">Position (EN)</label><input type="text" name="position_en" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Position (RU)</label><input type="text" name="position_ru" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Position (UZ)</label><input type="text" name="position_uz" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                        <div><label class="text-xs font-bold text-blue-400">Period (EN)</label><input type="text" name="period_en" placeholder="2024 – Present" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Period (RU)</label><input type="text" name="period_ru" placeholder="2024 – Настоящее время" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Period (UZ)</label><input type="text" name="period_uz" placeholder="2024 – Hozir" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                        <div><label class="text-xs font-bold text-blue-400">Description (EN)</label><textarea name="description_en" required rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"></textarea></div>
                        <div><label class="text-xs font-bold text-blue-400">Description (RU)</label><textarea name="description_ru" required rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"></textarea></div>
                        <div><label class="text-xs font-bold text-blue-400">Description (UZ)</label><textarea name="description_uz" required rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"></textarea></div>
                    </div>
                    <div class="flex items-center justify-between pt-2">
                        <div><label class="text-xs font-bold text-blue-400 mr-2">Company Website URL</label><input type="text" name="company_url" placeholder="https://..." class="bg-slate-900 border border-slate-700 rounded-xl px-3 py-1.5 text-sm text-white"></div>
                        <button type="submit" class="px-6 py-2 bg-blue-600 hover:bg-blue-500 rounded-xl font-bold text-xs text-white shadow-lg">Add Experience</button>
                    </div>
                </form>
            </div>

            <!-- Existing Experience List -->
            <div class="space-y-4 max-w-4xl">
                <% experience.forEach((exp) => { %>
                    <form action="/admin/experience/update/<%= exp.id %>" method="POST" class="bg-slate-800/40 p-4 md:p-6 rounded-2xl border border-slate-700 space-y-4">
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                            <div><label class="text-xs font-bold text-blue-400">Company (EN)</label><input type="text" name="company_en" value="<%= exp.company_en %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                            <div><label class="text-xs font-bold text-blue-400">Position (EN)</label><input type="text" name="position_en" value="<%= exp.position_en %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        </div>
                        <div class="flex justify-between">
                            <button type="submit" class="px-4 py-2 bg-blue-600 rounded-xl text-xs font-bold text-white">Update</button>
                            <button type="submit" formaction="/admin/experience/delete/<%= exp.id %>" class="px-4 py-2 bg-rose-600 rounded-xl text-xs font-bold text-white">Delete</button>
                        </div>
                    </form>
                <% }) %>
            </div>
        </div>

        <!-- Education Tab -->
        <div id="tab-education" class="tab-content space-y-6">
            <h2 class="text-xl md:text-2xl font-bold border-b border-slate-800 pb-3">Education Timeline</h2>
            
            <!-- Add New Education Form -->
            <div class="bg-blue-950/40 p-4 md:p-6 rounded-2xl border border-blue-500/30 mb-6 max-w-4xl">
                <h3 class="text-base md:text-lg font-bold text-blue-300 mb-3"><i class="fas fa-plus-circle mr-2"></i>Add New Education</h3>
                <form action="/admin/education/create" method="POST" class="space-y-4">
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                        <div><label class="text-xs font-bold text-blue-400">Institution (EN)</label><input type="text" name="institution_en" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Institution (RU)</label><input type="text" name="institution_ru" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Institution (UZ)</label><input type="text" name="institution_uz" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                        <div><label class="text-xs font-bold text-blue-400">Degree (EN)</label><input type="text" name="degree_en" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Degree (RU)</label><input type="text" name="degree_ru" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Degree (UZ)</label><input type="text" name="degree_uz" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                        <div><label class="text-xs font-bold text-blue-400">Period (EN)</label><input type="text" name="period_en" placeholder="2025 – 2029" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Period (RU)</label><input type="text" name="period_ru" placeholder="2025 – 2029" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        <div><label class="text-xs font-bold text-blue-400">Period (UZ)</label><input type="text" name="period_uz" placeholder="2025 – 2029" required class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                        <div><label class="text-xs font-bold text-blue-400">Description (EN)</label><textarea name="description_en" required rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"></textarea></div>
                        <div><label class="text-xs font-bold text-blue-400">Description (RU)</label><textarea name="description_ru" required rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"></textarea></div>
                        <div><label class="text-xs font-bold text-blue-400">Description (UZ)</label><textarea name="description_uz" required rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-2.5 text-sm text-white"></textarea></div>
                    </div>
                    <div class="flex items-center justify-between pt-2">
                        <div><label class="text-xs font-bold text-blue-400 mr-2">Institution Website URL</label><input type="text" name="institution_url" placeholder="https://..." class="bg-slate-900 border border-slate-700 rounded-xl px-3 py-1.5 text-sm text-white"></div>
                        <button type="submit" class="px-6 py-2 bg-blue-600 hover:bg-blue-500 rounded-xl font-bold text-xs text-white shadow-lg">Add Education</button>
                    </div>
                </form>
            </div>

            <!-- Existing Education List -->
            <div class="space-y-4 max-w-4xl">
                <% education.forEach((edu) => { %>
                    <form action="/admin/education/update/<%= edu.id %>" method="POST" class="bg-slate-800/40 p-4 md:p-6 rounded-2xl border border-slate-700 space-y-4">
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                            <div><label class="text-xs font-bold text-blue-400">Institution (EN)</label><input type="text" name="institution_en" value="<%= edu.institution_en %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                            <div><label class="text-xs font-bold text-blue-400">Degree (EN)</label><input type="text" name="degree_en" value="<%= edu.degree_en %>" class="w-full bg-slate-900 border border-slate-700 rounded-xl px-3 py-2 text-sm text-white"></div>
                        </div>
                        <div class="flex justify-between">
                            <button type="submit" class="px-4 py-2 bg-blue-600 rounded-xl text-xs font-bold text-white">Update</button>
                            <button type="submit" formaction="/admin/education/delete/<%= edu.id %>" class="px-4 py-2 bg-rose-600 rounded-xl text-xs font-bold text-white">Delete</button>
                        </div>
                    </form>
                <% }) %>
            </div>
        </div>
    </main>

    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            if (sidebar) {
                sidebar.classList.toggle('hidden');
                sidebar.classList.toggle('flex');
            }
        }

        function showTab(tabName) {
            document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));
            document.querySelectorAll('.nav-btn').forEach(el => el.classList.remove('bg-blue-600', 'text-white'));
            
            const activeTab = document.getElementById('tab-' + tabName);
            const activeNav = document.getElementById('nav-' + tabName);
            if (activeTab) activeTab.classList.add('active');
            if (activeNav) activeNav.classList.add('bg-blue-600', 'text-white');

            // Close sidebar on mobile selection
            if (window.innerWidth < 768) {
                const sidebar = document.getElementById('sidebar');
                if (sidebar && !sidebar.classList.contains('hidden')) {
                    sidebar.classList.add('hidden');
                    sidebar.classList.remove('flex');
                }
            }
        }

        const urlParams = new URLSearchParams(window.location.search);
        const initialTab = urlParams.get('tab') || 'settings';
        showTab(initialTab);
    </script>
</body>
</html>
